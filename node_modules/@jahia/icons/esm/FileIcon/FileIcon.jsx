function _objectWithoutProperties(source, excluded) { if (source == null) return {}; var target = _objectWithoutPropertiesLoose(source, excluded); var key, i; if (Object.getOwnPropertySymbols) { var sourceSymbolKeys = Object.getOwnPropertySymbols(source); for (i = 0; i < sourceSymbolKeys.length; i++) { key = sourceSymbolKeys[i]; if (excluded.indexOf(key) >= 0) continue; if (!Object.prototype.propertyIsEnumerable.call(source, key)) continue; target[key] = source[key]; } } return target; }

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

import React from 'react';
import PropTypes from 'prop-types';
import { File, FileDocument, FileExcel, FileImage, FileMusic, FilePdfBox, FilePowerpoint, FileVideo, FileWord, Folder } from 'mdi-material-ui';

var FileIcon = function FileIcon(_ref) {
  var filename = _ref.filename,
      props = _objectWithoutProperties(_ref, ["filename"]);

  switch (filename.split('.').pop().toLowerCase()) {
    case 'png':
    case 'jpeg':
    case 'jpg':
    case 'gif':
    case 'svg':
    case 'img':
      return React.createElement(FileImage, props);

    case 'avi':
    case 'mp4':
    case 'video':
      return React.createElement(FileVideo, props);

    case 'mp3':
    case 'aiff':
    case 'sound':
      return React.createElement(FileMusic, props);

    case 'pdf':
      return React.createElement(FilePdfBox, props);

    case 'gz':
    case 'tgz':
    case 'tar.gz':
    case 'jar':
    case 'rar':
    case 'zip':
      return React.createElement(File, props);

    case 'docx':
    case 'doc':
      return React.createElement(FileWord, props);

    case 'xlsx':
    case 'xls':
      return React.createElement(FileExcel, props);

    case 'pptx':
    case 'ppt':
      return React.createElement(FilePowerpoint, props);

    case 'css':
    case 'java':
    case 'html':
      return React.createElement(File, props);

    case 'txt':
    case 'csv':
    case 'text':
      return React.createElement(FileDocument, props);

    case 'folder':
      return React.createElement(Folder, props);

    default:
      return React.createElement(File, props);
  }
};

FileIcon.propTypes = {
  filename: PropTypes.string.isRequired
};
export default FileIcon;