import { dsGenericTheme } from '../../theme';
import { shallowWithTheme } from '../../../../test-framework';
import React from 'react';
import { Input } from './Input';
describe('Input', function () {
  var booleanProps = [{
    propName: 'readOnly',
    clazz: 'readOnly'
  }, {
    propName: 'disabled',
    clazz: 'inputDisabled'
  }, {
    propName: 'error',
    clazz: ''
  }];
  booleanProps.forEach(function (prop) {
    return it("should ".concat(prop.propName, " to be true on rendered component"), function () {
      var propName = prop.propName,
        clazz = prop.clazz;
      var props = [];
      props[propName] = true;
      var cmp = shallowWithTheme( /*#__PURE__*/React.createElement(Input, props), {}, dsGenericTheme).dive().dive();
      var cmpProps = cmp.props();
      expect(cmpProps[propName]).toBe(true);
      expect(cmpProps.className).toContain(clazz);
    });
  });
});