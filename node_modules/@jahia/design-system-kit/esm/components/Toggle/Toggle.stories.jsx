import React from 'react';
import { storiesOf } from '@storybook/react';
import { withKnobs, boolean as _boolean } from '@storybook/addon-knobs';
import { Toggle } from './Toggle';
import doc from './Toggle.md';
import { DSProvider } from '../../provider';
import { action } from '@storybook/addon-actions';
storiesOf('Toggle', module).addDecorator(withKnobs).add('default', function () {
  return /*#__PURE__*/React.createElement(DSProvider, null, /*#__PURE__*/React.createElement(Toggle, {
    checked: _boolean('checked', null),
    disabled: _boolean('disabled', false),
    readOnly: _boolean('readOnly', false),
    onBlur: action('onBlur'),
    onChange: action('onChange'),
    onFocus: action('onFocus')
  }));
}, {
  notes: {
    markdown: doc
  }
});