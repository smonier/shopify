import React from 'react';
import { shallowWithTheme } from '../../../../test-framework';
import { dsGenericTheme } from '../../theme';
import { Toggle } from './';
describe('Toggle', function () {
  it('should display a checked Toggle', function () {
    var cmp = shallowWithTheme( /*#__PURE__*/React.createElement(Toggle, {
      checked: true
    }), {}, dsGenericTheme).dive();
    expect(cmp.props().checked).toBe(true);
    expect(cmp.props().disabled).toBe(false);
  });
  it('should display an unchecked Toggle', function () {
    var cmp = shallowWithTheme( /*#__PURE__*/React.createElement(Toggle, {
      checked: false
    }), {}, dsGenericTheme).dive();
    expect(cmp.props().checked).toBe(false);
    expect(cmp.props().disabled).toBe(false);
  });
  it('should be disabled', function () {
    var cmp = shallowWithTheme( /*#__PURE__*/React.createElement(Toggle, {
      disabled: true
    }), {}, dsGenericTheme).dive();
    expect(cmp.props().disabled).toBe(true);
  });
  it('should be readonly', function () {
    var cmp = shallowWithTheme( /*#__PURE__*/React.createElement(Toggle, {
      readOnly: true
    }), {}, dsGenericTheme).dive();
    expect(cmp.props().disabled).toBe(true);
  });
  it('should update the checked attribute', function () {
    var toggleCmp = shallowWithTheme( /*#__PURE__*/React.createElement(Toggle, null), {}, dsGenericTheme);
    expect(toggleCmp.dive().props().checked).toBe(false);
    toggleCmp.setProps({
      checked: true
    });
    expect(toggleCmp.dive().props().checked).toBe(true);
  });
});