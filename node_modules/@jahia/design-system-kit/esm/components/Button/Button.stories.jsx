import React from 'react';
import { storiesOf } from '@storybook/react';
import Button from './Button';
import { DSProvider } from '../../provider';
import { action } from '@storybook/addon-actions';
import { boolean as _boolean, select, withKnobs } from '@storybook/addon-knobs';
import { Refresh } from '@material-ui/icons';
import doc from './Button.md';
var variant = function variant() {
  return select('variant', ['ghost', 'primary', 'secondary', 'inline'], Button.defaultProps.variant);
};
var color = function color() {
  return select('color', ['default', 'inverted'], Button.defaultProps.color);
};
var size = function size() {
  return _boolean('compact', false) ? 'compact' : 'normal';
};
storiesOf('Button', module).addDecorator(withKnobs).add('Simple button', function () {
  return /*#__PURE__*/React.createElement(DSProvider, null, /*#__PURE__*/React.createElement(Button, {
    variant: variant(),
    size: size(),
    color: color(),
    onClick: action('button-click')
  }, "Simple Button"));
}, {
  notes: {
    markdown: doc
  }
}).add('Button with icon', function () {
  return /*#__PURE__*/React.createElement(DSProvider, null, /*#__PURE__*/React.createElement(Button, {
    icon: /*#__PURE__*/React.createElement(Refresh, null),
    variant: variant(),
    size: size(),
    color: color(),
    onClick: action('button-click')
  }, "Button with icon"));
}, {
  notes: {
    markdown: doc
  }
});