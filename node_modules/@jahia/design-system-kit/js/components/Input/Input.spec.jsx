"use strict";

var _theme = require("../../theme");
var _testFramework = require("../../../../test-framework");
var _react = _interopRequireDefault(require("react"));
var _Input = require("./Input");
function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }
describe('Input', function () {
  var booleanProps = [{
    propName: 'readOnly',
    clazz: 'readOnly'
  }, {
    propName: 'disabled',
    clazz: 'inputDisabled'
  }, {
    propName: 'error',
    clazz: ''
  }];
  booleanProps.forEach(function (prop) {
    return it("should ".concat(prop.propName, " to be true on rendered component"), function () {
      var propName = prop.propName,
        clazz = prop.clazz;
      var props = [];
      props[propName] = true;
      var cmp = (0, _testFramework.shallowWithTheme)( /*#__PURE__*/_react["default"].createElement(_Input.Input, props), {}, _theme.dsGenericTheme).dive().dive();
      var cmpProps = cmp.props();
      expect(cmpProps[propName]).toBe(true);
      expect(cmpProps.className).toContain(clazz);
    });
  });
});