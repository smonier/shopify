"use strict";

var _composeActions = require("./composeActions");
describe('composeActions', function () {
  it('should call the tow action function', function () {
    var action = (0, _composeActions.composeActions)({
      // eslint-disable-next-line
      onInit: function onInit(context) {
        context.tata = 45;
      }
    }, {
      onInit: function onInit(context) {
        context.toto = 42;
      }
    });
    var context = {};
    action.onInit(context);
    expect(context).toEqual({
      toto: 42,
      tata: 45
    });
  });
});